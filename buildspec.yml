version: 0.2

batch:
  fast-fail: false
  build-graph:
    - identifier: without_db_tests
      env:
        compute-type: BUILD_GENERAL1_MEDIUM
        privileged-mode: true
      buildspec: ./buildspec_without_db.yaml
    - identifier: saas_tests
      env:
        compute-type: BUILD_GENERAL1_MEDIUM
        privileged-mode: true
      buildspec: ./buildspec_saas.yaml
    - identifier: onprem_tests
      env:
        compute-type: BUILD_GENERAL1_MEDIUM
        privileged-mode: true
      buildspec: ./buildspec_onprem.yaml
